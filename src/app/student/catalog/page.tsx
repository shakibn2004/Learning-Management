'use client';

import React from 'react';
import { StudentDashboard } from '../../../components/StudentDashboard';

export default function StudentCatalogPage() {
  return <StudentDashboard activeTab="courses" setActiveTab={() => {}} />;
}
