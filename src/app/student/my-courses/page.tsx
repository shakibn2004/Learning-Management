'use client';

import React from 'react';
import { StudentDashboard } from '../../../components/StudentDashboard';

export default function StudentMyCoursesPage() {
  return <StudentDashboard activeTab="my-courses" setActiveTab={() => {}} />;
}
